# VPS Environment Configuration for FTP Worker
# Copy this to .env on your VPS

# MongoDB Connection (use your MongoDB Atlas connection string)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# VPS Public IP Address (CRITICAL - must be your VPS's public IP)
VPS_PUBLIC_IP=198.251.79.28

# FTP Server Credentials
FTP_SERVER_USER=dealerscloud
FTP_SERVER_PASSWORD=Y8.>/TDWm0r#Pw0

# Node Environment
NODE_ENV=production
